import React from 'react'
import Auth from '../Auth'

interface Props {
    
}

const Header = (props: Props) => {
    return (
        <div>
           <Auth></Auth> 
        </div>
    )
}

export default Header
